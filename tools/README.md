# Custom tools directory

Drop user-created `.py` tools here. Read `../TOOLS.md` for the exact contract and required test procedure.

This directory is runtime data. Install/update preserves user-created tools rather than overwriting them.
